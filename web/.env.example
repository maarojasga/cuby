# URL pública del backend FastAPI (Render/Railway/HF Spaces).
# Si se deja vacío, el frontend funciona en modo demo con los datos empaquetados.
# Ej: https://cuby-api.onrender.com
NEXT_PUBLIC_API_URL=
